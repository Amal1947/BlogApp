<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BlogApp</title>
    <!-- Add Bootstrap CSS link -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <!-- Add Font Awesome link for icons (Use version 5.15.4) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        /* Custom styles for the navbar */
        body {
            margin: 0;
        }

        .navbar {
            background-color: #007bff;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 9999;
        }

        .navbar-brand {
            color: #fff;
            font-size: 24px;
            font-weight: bold;
        }

        .navbar-nav .nav-link {
            color: #fff;
            font-size: 18px;
            margin-right: 10px;
        }

        .navbar-nav .nav-link:hover {
            color: #fff;
        }

        .navbar-toggler {
            border-color: #fff;
        }

        .navbar-toggler-icon {
            background-image: url('data:image/svg+xml;charset=utf8,%3Csvg viewBox="0 0 32 32" xmlns="http://www.w3.org/2000/svg" width="32" height="32"%3E%3Cpath stroke="%23fff" stroke-miterlimit="10" stroke-width="2" d="M4 8h24M4 16h24M4 24h24"/%3E%3C/svg%3E');
        }

        .navbar-nav .btn-create-post {
            background-color: #fff;
            color: #007bff;
            border: none;
            border-radius: 5px;
            padding: 8px 16px;
            transition: all 0.3s ease-in-out;
        }

        .navbar-nav .btn-create-post:hover {
            background-color: #007bff;
            color: #fff;
        }

        .blog-card {
            border: 1px solid #ccc;
            border-radius: 5px;
            padding: 20px;
            margin-bottom: 20px;
            min-height: 200px;
            display: flex;
            flex-direction: column;
            position: relative;
            z-index: 0;
            width: 100%;
            margin-top: 30px;
        }

        .blog-title {
            font-size: 20px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .blog-content {
            color: #555;
            line-height: 1.5;
            flex-grow: 1;
            overflow: hidden;
        }

        .blog-content.full-content {
            overflow: visible;
        }

        .read-more {
            text-align: right;
        }

        /* Make the "Read More" link look like a button */
        .read-more-btn {
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            padding: 8px 16px;
            text-decoration: none;
            cursor: pointer;
            transition: background-color 0.3s ease-in-out;
        }

        .read-more-btn:hover {
            background-color: #0056b3;
        }

        /* Like and Comment section */
        .like-comment {
            display: inline-flex;
            margin-top: 15px;
        }

        .like-comment-btn {
            color: #555;
            cursor: pointer;
            margin-right: 10px;
        }

        /* Additional CSS for the navigation bar */
        .navbar {
            background-color: #007bff; /* Blue background color */
            padding-bottom: 1px;
        }

        .navbar-brand {
            color: #fff; /* White navbar brand text color */
            font-size: 24px;
            font-weight: bold;
        }

        .navbar a {
            color: #fff; /* White link color */
            font-size: 16px;
        }

        .navbar a:hover {
            background-color: #0056b3; /* Darker blue on hover */
            border-radius: 5px; /* Add rounded corners on hover */
        }

        .dropdown {
            position: relative;
            
            /* Set the position to relative for the dropdown container */
        }

        .dropdown-content {
            color: #000;
            box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.2);
            display: none;
            position: absolute;
            z-index: 1;
            top: 100%;
            right: 0;
            min-width: 200px; /* Set the minimum width to 200px (adjust as needed) */
        }

        /* Show dropdown content when the dropdown is active */
        .dropdown:hover .dropdown-content {
            display: block;
        }

        /* Custom book-open icon style */
        .book-open-icon {
            margin-right: 5px;
        }

        /* Custom door-open icon style */
        .door-open-icon {
            margin-right: 5px;
        }
        .blog-card:hover {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            transition: box-shadow 0.3s ease-in-out;
        }

        /* Display two cards horizontally */
        .container {
            display: flex;
            flex-wrap: wrap;
            
        }

        .blog-card {
            /* ... (existing styles for blog-card) ... */
            width: calc(50% - 30px); /* Adjust the width as needed */
            margin-right: 30px; /* Add some margin between the cards */
            
        }

        @media (max-width: 768px) {
            .blog-card {
                width: 100%; /* On smaller screens, display cards in full width */
                margin-right: 0;
            }
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container" >
            <!-- Brand/logo -->
            <a class="navbar-brand" href="#">
                <i class="fas fa-book-open book-open-icon"></i> BlogApp
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <!-- Added 'ml-auto' class here -->
                    <li class="nav-item">
                        <a class="nav-link" href="#home">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#contact">Contact</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#about">About</a>
                    </li>
                    <li class="nav-item dropdown"> <!-- Removed 'ml-auto' class here -->
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                            data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="fas fa-door-open door-open-icon"></i> Login
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end dropdown-content"
                            aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="/user/login" style="color: #000;">User Login</a></li>
                            <li><a class="dropdown-item" href="/tmanager/login" style="color: #000;">Topic Manager
                                    Login</a></li>
                            <li><a class="dropdown-item" href="/admin/login" style="color: #000;">Admin Login</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container pt-5">
      <% if (typeof res !== 'undefined' && res.length > 0) { %>
        <% res.forEach((blogPost) => { %>
        <!-- Blog Card -->
        <div class="blog-card">
            <div class="card-body">
                <div class="postName">Posted by <%= blogPost.name %></div>
                <h5 class="card-title blog-title topicc"><%= blogPost.topic %></h5>
                <div class="postDate">Posted on: <%= blogPost.date %></div>
                <div class="card-text blog-content discription">
                    <%= blogPost.articleDiscription %>
                </div>
                <div class="read-more">
                    <a href="/user/login"><button class="read-more-btn">Read More</button></a>
                </div>
                <!-- <div class="like-comment">
                    <button class="like-comment-btn"><i class="far fa-thumbs-up"></i> Like</button>
                    <button class="like-comment-btn"><i class="far fa-comment"></i> Comment</button>
                </div> -->
            </div>
        </div>
        <% }); %>
    <% } else { %>
        <div><%= man.message %></div>
    <% } %>
        <!-- Your blog card content -->
    </div>

    <!-- Add Bootstrap JS script -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.0.7/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <!-- Add Font Awesome JS script (Use version 5.15.4) -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/js/all.min.js"></script>
</body>

</html>
